# 比赛演示视频最终动作表（2026-04-22）

| 时长 | 账号 | 动作 | 要说的话 |
| --- | --- | --- | --- |
| `0:00–0:15` | 未登录 | 打开 `campusbook.top`，按一次 `Tab`，让“跳到主要内容”露出来；镜头停 `1` 秒，把“学生入口 / 教师入口”都带到 | `这套系统有学生端和教师工作台两个入口，但不是两个独立系统，而是在同一平台里处理不同业务角色。` |
| `0:15–0:25` | 学生 | 点击“学生入口”进入系统 | `我先用学生视角演示三类核心业务，再用教师视角证明这些限制和状态来自后台规则，而不是写死在页面里。` |
| `0:25–1:15` | 学生 | 进入“学术空间预约”，选 `创新协作室 A`，先填 `明天 10:00 - 12:01` 提交失败；改成 `10:00 - 11:00` 成功；再回去填 `11:04 - 12:04` 触发冲突；回订单详情点“取消预约” | `学术空间是连续时间预约，不是整点 slot。这里先看规则限制，再看成功预约，最后故意贴着上一单再约，系统会按缓冲区拦截，取消后订单状态也会立刻回写。` |
| `1:15–1:55` | 学生 | 进入“体育馆预约”，选 `羽毛球馆 A 区`，切“组合预订”，选 `双场联动场地`，点 `明天 10:00` 提交；回时间表停 `2` 秒；切回“单场地”指同一时段 | `体育设施和学术空间是另一套模型。这里是严格 1 小时 slot，而且一次锁的是组合资源，所以同一时段下 A1 和 A2 会一起被占用，不会出现静默部分成功。` |
| `1:55–2:50` | 学生 | 进入“校园活动”，选 `校园开放日 2026`，点击 `优先通行名额` 的“立即报名”；停在“我的报名状态”等待；状态变“待确认”后点“查看订单详情”；停 `2` 秒看金额、支付状态、截止时间；点“模拟支付” | `活动链路不是点一下就直接写库，而是先排队，再进入待支付状态。这里可以看到订单先是待确认，带支付截止时间，模拟支付后才转成已确认。` |
| `2:50–3:05` | 学生 | 回到首页或直接退出登录 | `前面三条链路都是学生可见的产品结果，但这些限制和状态并不是前端自己判断出来的。` |
| `3:05–3:30` | 管理员 | 点击“教师入口”，进入“教师工作台”；点“规则配置”，切到“学术”，点开 `学术空间最长预约时长` | `教师端这里不是普通后台表单，它直接对应学生端刚才看到的规则限制。也就是说，前台拦截和后台配置是同一套业务规则。` |
| `3:30–3:45` | 管理员 | 停在规则配置页收尾 | `所以这套系统的重点不是把页面点通，而是针对学术、体育、活动三类业务，用不同的领域模型、规则和状态机处理边界问题。` |
